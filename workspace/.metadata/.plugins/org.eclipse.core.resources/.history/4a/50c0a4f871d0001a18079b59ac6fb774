package programs;

import java.util.Scanner;

public class Time3 {
	public static void main(String[] args) {
		System.out.println("Enter a four digit number");
		Scanner scn =new Scanner(System.in);
		String num=scn.next();
		int hh;
		int mm;
			String x = num.substring(0, 2);
			String y= num.substring(2);
			System.out.println(x +" and "+ y);
			StringBuilder a= new StringBuilder(x);
			StringBuilder b= new StringBuilder(y);
			if(Integer.parseInt(x)>Integer.parseInt(a.reverse().toString()))
					{
						int hh = Integer.parseInt(x);
					}
			else
			{
				int hh = Integer.parseInt(a.reverse().toString());
			}
			if(Integer.parseInt(y)>Integer.parseInt(b.reverse().toString()))
			{
				int mm = Integer.parseInt(y);
			}
			else
			{
				int mm = Integer.parseInt(b.reverse().toString());
			}
			if ((hh<24)&&(mm<60))
			{
				System.out.println("The highest time is "+hh+":"+mm);
				
			}
			else if((hh>24)&&(mm<60))
			{
				hh=00;
				System.out.println("The highest time is "+hh+":"+mm);
			}
			else if((hh<24)&&(mm>60))
			{
				mm=00;
				System.out.println("The highest time is "+hh+":"+mm);
			}
*/
}
}
